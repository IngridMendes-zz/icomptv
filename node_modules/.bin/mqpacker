#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css-mqpacker/bin/mqpacker" "$@"
  ret=$?
else 
  node  "$basedir/../css-mqpacker/bin/mqpacker" "$@"
  ret=$?
fi
exit $ret
